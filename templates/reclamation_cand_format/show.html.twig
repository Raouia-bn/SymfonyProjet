{% extends 'base.html.twig' %}

{% block title %}ReclamationCandFormat{% endblock %}

{% block body %}
    <h1>ReclamationCandFormat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation_cand_format.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation_cand_format.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamation_cand_format_index') }}">back to list</a>

    <a href="{{ path('reclamation_cand_format_edit', {'id': reclamation_cand_format.id}) }}">edit</a>

    {{ include('reclamation_cand_format/_delete_form.html.twig') }}
{% endblock %}
