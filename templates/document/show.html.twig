{% extends 'base_cand_interfaces.html.twig' %}

{% block title %}Document{% endblock %}

{% block body %}
    <h1>Documents</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ document.id }}</td>
        </tr>
        <tr>
            <th>Designation</th>
            <td>{{ document.designation }}</td>
        </tr>
        <tr>
            <th>Formation</th>
            <td>{{ document.formation.nomf }}</td>
        </tr>
        </tbody>

    </table>

    <a href="{{ path('documents_show', {'id': document.formation.id})}}">back to list</a>

    <a href="{{ path('document_edit', {'id': document.id}) }}">edit</a>

    {{ include('document/_delete_form.html.twig') }}
{% endblock %}
