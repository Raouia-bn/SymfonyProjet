{% extends 'base_cand_interfaces.html.twig' %}
{% block body %}
    <div class="my-3">
        <div class="container">
            <div class="row">
                <div class="col-md-12"><img class="img-fluid d-block rounded-circle w-25 mx-auto" src="../../images/logo-trabsparente.png"></div>
            </div>
        </div>
    </div>
    <div class="">
        <div class="container">
            <div class="row">
                <div class="col-md-12 py-3">
                    <h3 class="display-3 text-center text-capitalize" style="">Description for project</h3>
                </div>
            </div>
        </div>
    </div>
    <div class="">
        <div class="container">
            <div class="row">
                <div class="text-right col-md-12" align="right">
                    <form class="form-inline" align="right">
                        <div class="input-group" align="right">
                            <input type="text" class="form-control form-control-lg" id="inlineFormInputGroup" placeholder="Search" align="right">
                            <div class="input-group-append" align="right"><button class="btn btn-primary" type="button" align="right"><i class="fa fa-search"></i></button></div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <div class="py-3" style="">
    <div class="container">
    <div class="row">
    <div class="table-responsive" style="">
    <table class="table">
    <thead>
    <tr>
        <th>id</th>
        <th>Description</td>
        <th>Project</th>
        <th>Actions</th>
    </tr>
    <thead>
    {% for l in listDocument %}
        <tr>
            <td>{{ l.id }}</td>
            <td>{{ l.designation }}</td>
            <td>{{ l.formation.nomf }}</td>
            <td>

                <div class="col-md-12">
                    <a href="{{ path('document_show', {'id': l.id}) }}" class="btn btn-outline-info" style="">Show</a>
                    <a href="{{ path('document_edit', {'id': l.id}) }}" class="btn btn-outline-primary">Edit</a>
                    <a href="{{ path('document_show', {'id': l.id}) }}" class="btn btn-danger">Delete</a>
                </div>

            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="7">no records found</td>
        </tr>
    {% endfor %}
</table>
        <a class="btn btn-primary pi-draggable text-white" href="{{ path('document_new') }}">Import</a>

    </div>
        <a href="{{ path('formationFormateur_index', {'id': project.id}) }}">back to list</a>
    </div>
    </div>
    </div>

{% endblock %}