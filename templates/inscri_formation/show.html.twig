{% extends 'base_cand_interfaces.html.twig' %}

{% block title %}InscriFormation{% endblock %}

{% block body %}
    <h1>InscriFormation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscri_formation.id }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ inscri_formation.etat }}</td>
            </tr>
            <tr>
                <th>NomFormation</th>
                <td>{{ inscri_formation.NomFormation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inscri_formation_index') }}">back to list</a>

    <a href="{{ path('inscri_formation_edit', {'id': inscri_formation.id}) }}">edit</a>

    {{ include('inscri_formation/_delete_form.html.twig') }}
{% endblock %}
