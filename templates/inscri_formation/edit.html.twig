{% extends 'base_cand_interfaces.html.twig' %}

{% block title %}Edit InscriFormation{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-4">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h1 class="mb-0">Edit Demande</h1>
                    </div>
                    <div class="card-body">

                        {{ include('inscri_formation/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                </div>

                <div class="mt-3">
                    <a href="{{ path('inscri_formation_index') }}" class="btn btn-secondary">Back to List</a>
                </div>

                <div class="mt-3">
                    <a href="{{ path('inscri_formation_delete', {'id': inscri_formation.id}) }}" class="btn btn-danger">Delete</a>
                </div>


            </div>
        </div>
    </div>
{% endblock %}
